import 'package:flutter/material.dart';

/// Application colour palette.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFB6A192);
  static const Color appBarBackground = Color(0xFF706E6E);
  static const Color priceText = Color(0xFF182525);
}
